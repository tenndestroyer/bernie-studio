"""Core utilities: structured events bus and typed state wrappers.

These are additive, backward-compatible helpers. The existing pipeline keeps working with raw
JSON dicts; this package gives the GUI and tests a typed, robust interface to the same artifacts.
"""
