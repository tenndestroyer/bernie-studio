# Pipeline deps (the heavy stuff — ComfyUI, PyTorch cu128, models — is installed by setup.ps1).
# These are the lightweight host-side helpers:
edge-tts>=6.1
pillow>=10.0
numpy>=1.26
huggingface_hub>=0.23
py7zr>=0.21
